# Model diagnostics helpers for the HindiBabyNet R workflow.
#
# Phase 1 scaffold only. Later phases will add residual, QQ, convergence,
# singularity, and DHARMa diagnostics.

run_model_diagnostics <- function(model, output_dir) {
  stop("Not implemented yet: run_model_diagnostics().", call. = FALSE)
}
