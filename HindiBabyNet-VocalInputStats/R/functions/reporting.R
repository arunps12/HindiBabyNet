# Reporting helpers for the HindiBabyNet R workflow.
#
# Phase 1 scaffold only. Later phases will add model table export, effect
# summaries, and evidence-based interpretation helpers.

build_reporting_bundle <- function(models, outputs) {
  stop("Not implemented yet: build_reporting_bundle().", call. = FALSE)
}
