# Prediction plotting helpers for the HindiBabyNet R workflow.
#
# Phase 1 scaffold only. Later phases will add reusable prediction and figure
# functions that predict on age_z and display age_days.

plot_prediction_with_ci <- function(prediction_data, x_var, y_var, output_stub) {
  stop("Not implemented yet: plot_prediction_with_ci().", call. = FALSE)
}
