# HindiBabyNet-ProsodyModel

Prosodic prominence model training and evaluation for the HindiBabyNet project.

## Setup

```bash
uv sync
uv run python -c "import hindibabynet_prosodymodel"
```

## Data

Expects annotated prosodic data under `data/`.

## Usage

See `scripts/` for training and evaluation entry points.
